package StaticDataMembers;

public class Bank {

	int accNumber;			//instance variable
	static int ifsccode;	//class variable
	
	static {
		System.out.println("Static block.....");
	}
	/*
	 * static block executes only once 
	 * also it invokes before constructor*/
	Bank(){
		System.out.println("it's constructor");
	}
	
	public void show() {
		System.out.println("=================================");
		System.out.println("Value of acc number is "+ accNumber);
		System.out.println("Value of ifsc Code  is "+ ifsccode);
	}
	
	public static void display() {
		Bank temp=new Bank();
		System.out.println("Value of acc number is "+ temp.accNumber); 
		// in static method we can only use static variable 
	//if we want to use the instance variable then we need to create the class object first like above
		System.out.println("Value of ifsc Code  is "+ ifsccode);
	}
}
